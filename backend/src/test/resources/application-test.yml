spring:
  datasource:
    url: @testDataSource.url@
    username: @testDataSource.user@
    password: @testDataSource.password@
    dbcp2:
      max-total: 1
      max-idle: 1
  jpa:
    hibernate:
      ddl-auto: none